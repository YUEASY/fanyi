# edge翻译插件

## 目标

这个插件解决什么问题？

解决程序员需要在文档中学习英文的需求

目标用户是谁？

英文能力只有小学水平的程序员

典型使用场景是什么？

阅读英文技术网页时，自动识别并高亮不认识的单词，减少频繁复制翻译。

（初始按小学生水平，后续可以将单词移除生词本，加入熟词本）

**不在熟词表 → 视为潜在生词并高亮，但不自动加入生词本。**
 仅用户点击“加入生词本”时才写入生词本；单纯悬停只展示翻译，不写数据。

熟词表优先级最高；即使词典或专业术语表里存在该词，只要已标记为熟词，就不再高亮。

小学水平：`google-10000-english` 截前 1500 个

## 核心流程

打开英文网页

插件扫描页面，判断单词是否在熟词表中

自动突出生词（可以是下划线）

鼠标悬停查看翻译，

点击“认识”

加入个人熟词表，该单词立即取消当前网页中所有相同单词的黄色下划线，刷新或访问其他网页仍然不再高亮。

以后再访问网页时根据词表决定是否突出

或者点击“专业术语翻译”，交给llm来翻译（用户需要自行配置 API Key）

## 生词怎么判断

数字、代码、URL、变量名不处理

熟词表永远不标记

## 翻译规则

优先翻译单词，用户也可以使用鼠标选中短语后来翻译

不要显示音标，不要显示词性

专业术语可以让用户选择交给llm来翻译，显示优先于基础翻译

词形变化怎么处理：尽可能做基础词形还原，熟词表保存原形；无法识别时按独立单词处理。

默认扫描正文文本，忽略 code/pre/script/style/input/textarea，以及不可见文本。

交互

生词：黄色下划线

鼠标悬停： embedded

[认识] [加入生词本]

## 翻译来源

普通单词 → 本地/在线词典；专业术语 → 用户主动选择后调用 LLM。

## 页面动态变化

 Hyperskill、GitHub 这类网页有时会动态加载内容。建议加：

> 页面新增文本时，插件应继续扫描新增内容，而不是只在首次打开页面时扫描。

## 边界情况

一个词出现 50 次全部高亮，但数据只存一份

`Spring Boot` 这种词组怎么处理？优先按单词来，用户可选交给llm翻译

## 开关

> 用户可以对当前网站开启/关闭自动高亮。

## MVP 不做什么

OCR

Windows 全局翻译

登录

云同步

Anki 同步

AI 自动生成例句

复杂复习算法

手机端

## 成功标准

在 Hyperskill、GitHub Docs 等英文技术网页上，可以自动高亮生词；用户能悬停查看翻译，并将单词标记为已掌握；刷新页面后状态仍然保留。

